# BrewApps-Assignment

# step 1 
    run "npm i" on the terminal for download the dependency

# step 2
    run "npm start" on terminal for start the server

# step 3
    check all the api's using postman or other